import 'package:flutter/material.dart';

/// 主要色
const Color primaryColor = Color.fromRGBO(67, 142, 219, 1);

/// 次要色
const Color secondaryColor = Color.fromRGBO(192, 210, 240, 1);

const Color navigationBarColor = Colors.white;

/// 默认大小
const double primaryFontSize = 14;

const double secondaryFontSize = 12;

const double sunnyTextSize = 30;

/// 主题颜色
const MaterialColor themeBlue = MaterialColor(
  0xFF438EDB,
  <int, Color>{
    50: Color(0xFF438EDB),
    100: Color(0xFF438EDB),
    200: Color(0xFF438EDB),
    300: Color(0xFF438EDB),
    400: Color(0xFF438EDB),
    500: Color(0xFF438EDB),
    600: Color(0xFF438EDB),
    700: Color(0xFF438EDB),
    800: Color(0xFF438EDB),
    900: Color(0xFF438EDB),
  },
);

/// 颜色选项